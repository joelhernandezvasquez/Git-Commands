 git branch <name>: It creates a new branch
 git checkout <name of the branch>: it switch to the branch specified
 git merge <name of the branch>: It merge your branch
 git branch -m : it changes the name of a branch. For example git branch -m master main
 git branch -d >branch name>: It deletes a branch
 git checkout -b <name of the branch>: it creates and move to the branch at the same time.



